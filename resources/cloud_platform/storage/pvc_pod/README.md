# PVC Pod Helm Chart Resource

Place the `availability-test-pods-0.2.0.tgz` helm chart file in this directory.

This chart is used by the PVC pod automation to deploy availability test pods
via `helm upgrade --install`.
